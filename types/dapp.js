export default class Dapp {
	constructor(
		name, 
		logo,
		description,
		uri,
	) {
        this.name = name;
        this.logo = logo;
        this.description = description;
        this.uri = uri;
    }
} 